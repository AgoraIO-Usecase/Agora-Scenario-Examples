<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="io.agora.scene.livepk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:icon="@mipmap/pk_ic_launcher"
        android:label="@string/pk_app_name"
        android:roundIcon="@mipmap/pk_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/pk_Theme.AgoraSceneAndroid"
        tools:ignore="AllowBackup">

        <activity
            android:name=".activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.LivePKListActivity" />
        <activity android:name=".activity.HostPKActivity" />
        <activity android:name=".activity.AudienceActivity" />

    </application>

</manifest>